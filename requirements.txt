# Core dependencies
streamlit>=1.20.0,<2.0.0
langchain>=0.0.0,<1.0.0
faiss-cpu>=1.7.0,<2.0.0
PyPDF2>=3.0.0,<4.0.0
numpy>=1.21.0,<1.27.0
tiktoken>=0.4.0,<0.5.0

# Optional: embedding models / LLMs (OpenAI)
openai>=0.27.0,<1.0.0

# Optional: local environment helpers (for loading .env, etc.)
python-dotenv>=1.0.0,<2.0.0

# Optional: GPU-accelerated FAISS (if you have a compatible GPU)
# faiss-gpu>=1.7.0,<2.0.0